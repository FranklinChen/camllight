name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      # Keep building every OS even when one fails, so a Linux break never
      # hides the macOS result (and vice versa) while we diagnose.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v6

    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential

    - name: Configure and build
      run: |
        cd sources/src
        make configure
        make OPTS="-O2 -Wno-implicit-int -Wno-deprecated-non-prototype -Wno-pointer-sign -Wno-implicit-function-declaration" world

    - name: Bootstrap
      run: |
        cd sources/src
        make OPTS="-O2 -Wno-implicit-int -Wno-deprecated-non-prototype -Wno-pointer-sign -Wno-implicit-function-declaration" bootstrap

    - name: Test basic functionality
      run: |
        cd sources/src
        echo "1 + 2;;" | ./camlrun toplevel/camltop -stdlib lib
